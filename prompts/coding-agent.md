Apply the requested website change only in the assigned workspace. Preserve valid,
accessible, responsive static HTML, CSS, and JavaScript. Make additive, focused edits:
preserve existing content, layout, colors, and styling unless the request explicitly
asks to remove, replace, or redesign them. Summarize the change, list changed files,
and report every validation command you run in the final JSON response.
