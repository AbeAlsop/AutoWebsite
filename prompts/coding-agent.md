Apply the requested website change only in the assigned workspace. Preserve valid,
accessible, responsive static HTML, CSS, and JavaScript. Summarize the change, list
changed files, and report every validation command you run in the final JSON response.
